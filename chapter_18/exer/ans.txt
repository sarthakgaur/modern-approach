1.
    S.No storage, qualifiers, specifiers, declarators, initilizers
    a. static, , char **, lookup(int level), ,;
    b.  , volatile, unsigned long, io_flags, ,;
    c. extern, , char*, (file_name[MAX_FILES], path[]), ,;
    d. static, const, char, token_buf[], "";

2.
    a. extern
    b. static
    c. static. // Wrong. extern should be added too.

3.
    a. static, file, ?;
    b. auto, block, none;
    c. static, block, none;
    d. auto, block, none;

4. f(10) -> 0, after 5 calls f(10) -> 50

5.
    a. False. Static variables can be declared inside function, and function variables have block scope.
    b. True. Variables declared inside functions are local to functions. They cannot be accessed outside of it.
    c. True. static keyword needs to present on variable definitions for it to have internal linkage.
    d. True. Parameter names cannot be accessed outside of its functions.

6.
    void print_error(const char *message) {
        static int n = 1;
        printf("Error %d: %s\n", n++, message);
    }

7.
    a. False.
    b. True.
    c. True.
    d. True.

8.
    a. x is an array of function pointers that have an int argument and return char.
    b. x is a function with int parameter and returns a pointer to an int array.
    c. x is a function with no parameters returning a pointer to a function with an int parameter and returns float *.
    d. x is a function with two parameters, an int and a pointer to a function that has an int parameter and returns nothing. x returns a pointer to a function that has an int parameter and return nothing.

9.
    a.
        typedef char f(int); // Wrong.
        typedef f *z;
        typedef z[10];

    b.
        typedef int x[5]; // Wrong.
        typedef x *y(int);

    c.
        typedef float *f(int); // Wrong.
        typedef f *x(void);

    d.
        typedef void z(int); // Wrong.
        typedef void y(int);
        typedef func_z *x(int, *func_y)

10.
    a. char *(*p) (char *);
    b. void (*f(struct t *p, long int n)) (void); // Wrong.
    c. void (*a[4]) (void) = {insert, search, update, print};
    d. struct t (*b[10]) (int, int);

11.
    a. int *f (int);
    b. (int (*f)(int))(int); // Wrong.
    c. int (*f[10]) (int);

12.
    a.
        1. f is a function.
        2. with two parameters:
            2a. a pointer to a function with long parameter and return float
            2b. a char pointer
        3. and return a pointer to a function with double parameter and return int.

    b. 
        float a(long) {};
        int b(double) {};
        char ch = 'a'
        f(a, &ch); // Wrong.


13.
    a. Legal.
    b. Not Legal.
    c. Legal.
    d. Legal.

14. e. None of the above.

15. a. Storage duration
